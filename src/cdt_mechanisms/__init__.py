"""Bottleneck, scale, persistence, intervention, and operator experiments.

Synthetic method validation is deliberately distinct from CDT ensemble evidence.
This additive package does not change the simulator or the existing research gates.
"""
__version__ = "0.1.0"
